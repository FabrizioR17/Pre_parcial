DROP TABLE IF EXISTS COSA; CREATE TABLE COSA(ID LONG AUTO_INCREMENT PRIMARY KEY, CODIGO INT NOT NULL, NOMBRE VARCHAR(50) NOT NULL, LABORATORIO VARCHAR(50) NOT NULL, CANTIDAD INT NOT NULL, PRECIO NUMERIC(10, 2) NOT NULL)